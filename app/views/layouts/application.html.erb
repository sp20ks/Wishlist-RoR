<!DOCTYPE html>
<html>
  <head>
    <title>Wishlist</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <header>
      <nav class="navbar-expand-md navbar navbar-light border border-left-0 border-right-0 rounded-bottom" style="background-color: #e3f2fd;">
        <div class="container-fluid">
          <a class="navbar-brand abs" href="#">Wishlist</a>
          <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#collapseNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="navbar-collapse collapse" id="collapseNavbar">
            <ul class="navbar-nav ms-auto">
              <%if user_signed_in?%>
                <li class="nav-item">
                  <%= link_to 'Личный кабинет', info_user_path, class: 'nav-link', id: 'profile'%>               
                </li>
                <li class="nav-item">
                  <%= link_to 'Выход', exit_path, class: 'nav-link', id: 'exit'%>               
                </li>
              <%else%>
                <li class="nav-item">
                  <%= link_to 'О проекте', class: 'nav-link', id: 'about'%>               
                </li>
              <%end%>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <div class="content">
        <% flash.each do |type, msg| %>
          <div class='alert alert-info' id='flash'>
            <%= msg %>
          </div>
          <% flash.clear %>
        <% end %>
    <%= yield %>
    </div>

    <footer class="bg-light bd-footer text-center border border-left-0 border-right-0 rounded-top">
      <div class="text-center p-3" style="background-color: #e3f2fd;">
        Зачетная работа по языкам интернет-программирования
        <a class="text-dark" href="#">Sp20ks</a>
      </div>
    </footer>
  </body>

</html>
